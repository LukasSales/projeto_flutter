name: flutter_projeto_live
description: "Projeto em Flutter para criar aplicativo"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  file_picker: ^4.0.0
  http: ^0.13.3
  permission_handler: ^9.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
flutter:

  uses-material-design: true

  assets:
    - assets/